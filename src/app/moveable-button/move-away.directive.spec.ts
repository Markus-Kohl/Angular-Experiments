import { MoveAwayDirective } from './move-away.directive';

describe('MoveAwayDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveAwayDirective();
    expect(directive).toBeTruthy();
  });
});
