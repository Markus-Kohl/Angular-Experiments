import { ClearDirective } from './clear.directive';

describe('ClearDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearDirective();
    expect(directive).toBeTruthy();
  });
});
